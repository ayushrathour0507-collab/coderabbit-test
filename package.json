{
  "name": "cursor-coderabbit-integration-test",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal project to validate Cursor + CodeRabbit integration on pull requests.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts",
    "check": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.30.0",
    "@typescript-eslint/parser": "^8.30.0",
    "eslint": "^9.23.0",
    "typescript": "^5.8.3",
    "vitest": "^3.0.8"
  }
}
